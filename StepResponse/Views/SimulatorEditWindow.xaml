<Window x:Class="StepResponse.Views.SimulatorEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StepResponse.Views"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="Edit Simulator" Height="400" Width="400">
    <StackPanel Margin="10">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Height="28">
            <xctk:ColorPicker SelectedColor="{Binding Color, Mode=TwoWay}" Width="40" Height="28" />
            <ComboBox ItemsSource="{Binding ModelTypes}" SelectedItem="{Binding ModelType, Mode=TwoWay}" Height="20" Margin="10,0,0,0" Width="150"/>
            <CheckBox IsChecked="{Binding UsePid, Mode=TwoWay}" Content="Use PID" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            
        <TextBlock Text="Setpoint:" Width="55"/>
        <TextBox Text="{Binding Setpoint, Mode=TwoWay}" Width="100"/>
        </StackPanel>

        <TextBlock Text="Model Parameters:" Margin="0,10,0,0"/>
        <DataGrid ItemsSource="{Binding ModelParameters}" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" Height="100">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Key}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Value" Binding="{Binding Value, Mode=TwoWay}"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Text="PID Parameters:" Margin="0,10,0,0"/>
        <DataGrid ItemsSource="{Binding PidParameters}" IsEnabled="{Binding UsePid, Mode=OneWay}" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" Height="100">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Key}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Value" Binding="{Binding Value, Mode=TwoWay}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="80" Margin="0,0,5,0" Click="Ok_Click"/>
            <Button Content="Cancel" Width="80" Click="Cancel_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
